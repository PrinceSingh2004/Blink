# ── BLINK PRODUCTION ENGINE CONFIG (RENDER v1.0) ────────────
# Copy these keys into your Render Dashboard → Service → Environment

NODE_ENV=production
PORT=4000

# Railway MySQL Database (External TCP)
DB_HOST=gondola.proxy.rlwy.net
DB_PORT=49958
DB_USER=root
DB_PASSWORD=YOUR_RAILWAY_PASSWORD
DB_NAME=railway

# Security & Authentication
JWT_SECRET=YOUR_SECURE_TOKEN_HERE

# Cloudinary Media API (MUST MATCH CONFIG)
# Dashboard → Settings → API Keys
CLOUDINARY_NAME=dvnvx42na
CLOUDINARY_API_KEY=949983528816724
CLOUDINARY_API_SECRET=5X-dHAaOU4GEPmiJmy2xrvOGQ0U

# CORS Settings
ALLOWED_ORIGINS=https://blink-yzoo.onrender.com,http://localhost:3000
